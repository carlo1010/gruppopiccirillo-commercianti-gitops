apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tenant-server-state
  namespace: platform
  labels:
    app.kubernetes.io/part-of: tenant-platform
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
