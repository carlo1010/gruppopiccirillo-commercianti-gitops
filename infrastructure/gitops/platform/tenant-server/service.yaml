apiVersion: v1
kind: Service
metadata:
  name: tenant-server
  namespace: platform
  labels:
    app.kubernetes.io/part-of: tenant-platform
    app.kubernetes.io/name: tenant-server
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: tenant-server
  ports:
    - name: http
      port: 80
      targetPort: 3001
      protocol: TCP
